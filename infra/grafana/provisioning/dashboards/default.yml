apiVersion: 1

providers:
  - name: "Server Health Checker"
    orgId: 1
    folder: "SHC Platform"
    folderUid: "shc-folder"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
